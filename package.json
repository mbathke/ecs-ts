{
  "name": "ecs-ts",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "clean": "rimraf build",
    "prebuild": "npm run clean",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Bathke",
  "license": "GPL-3.0-or-later",
  "description": "",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
